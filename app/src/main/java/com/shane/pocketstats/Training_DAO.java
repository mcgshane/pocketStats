package com.shane.pocketstats;

import androidx.room.Dao;
import androidx.room.Query;

import java.util.List;

@Dao
public interface Training_DAO {

  //  @Query("SELECT * FROM stats WHERE ")
   // List<DB_Entity_Stats> getAll();

    //  @Query("SELECT * FROM training WHERE type = :linkType")
    //    List<DB_Entity_Training> getLinks(String linkType);
}